CREATE TABLE patient (
                         age    INT ,
                         id   bigint auto_increment PRIMARY KEY,
                         gender  VARCHAR(255) ,
                         name    VARCHAR(255) ,
                         surname VARCHAR(255) ,
                         email VARCHAR(255)
);